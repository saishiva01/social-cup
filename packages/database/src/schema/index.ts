/**
 * Drizzle table definitions live here, one file per domain area
 * (e.g. `users.ts`, `memberships.ts`, `credits.ts`, `redemptions.ts`),
 * re-exported from this barrel file.
 *
 * No tables are defined yet — the Social Cup schema is intentionally out of
 * scope for this phase (see docs/architecture/database-architecture.md).
 * Add a schema file here, export it below, then run `pnpm db:generate` to
 * produce the corresponding migration.
 */
export {};
